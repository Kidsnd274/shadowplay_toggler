name: shadowplay_toggler
description: "ShadowPlay Toggler - manage per-app ShadowPlay/Overlay exclusions."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  desktop_drop: ^0.7.0
  ffi: ^2.2.0
  file_picker: ^11.0.2
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  path: ^1.9.0
  path_provider: ^2.1.5
  sqflite_common_ffi: ^2.3.4+4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.5

flutter:
  uses-material-design: true
